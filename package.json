{
  "name": "hedron-api",
  "version": "1.0.0",
  "description": "REST API for pulling HEX Stake Instance (HSI) NFT data from the blockchain.",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "MIT",
  "dependencies": {
    "cors": "^2.8.5",
    "ethers": "^5.5.4",
    "express": "^4.17.2",
    "express-winston": "^4.2.0",
    "winston": "^3.6.0"
  },
  "devDependencies": {
    "eslint": "^8.8.0"
  }
}
